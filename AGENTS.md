# 基本方針

- 必ず日本語で応対してください。
- ユーザーは音声入力を使用しているため、プロンプトに誤字・誤変換がある場合は音声認識の誤検知として解釈する（例：「Cloud Code」→「Claude Code」）。
- ユーザーは日本時間（JST / UTC+9）で生活している。曜日や時間帯に言及する際はJSTで解釈・表現すること。

# Claude Code関連

- コンテキスト節約のため、調査やデバッグにはサブエージェントを活用してください。

# ワーク開始ワークフロー

ワークを開始するときは、必ず `works/` ディレクトリ直下にワーク名のディレクトリを作り、以下の4つのファイルを使いながら作業を進める：

| ファイル | 役割 | 作成タイミング |
|---------|------|--------------|
| `works/<work>/plan.md` | ユーザーが音声・テキストで話した要件・アイデア・背景 | ユーザーが最初に記載 |
| `works/<work>/spec.md` | plan.md をもとに Claude が整理した仕様・設計・ユーザーへの確認事項 | plan.md 受け取り後に Claude が作成 |
| `works/<work>/todo.md` | 作業進捗・タスク一覧・未解決の課題（`/clear` 後も作業再開できるよう常時更新） | 作業開始後に Claude が作成・随時更新 |
| `works/<work>/knowledge.md` | ハマりポイント・バグ・解決策・仕様の罠などのナレッジ蓄積 | 作業中に Claude が随時追記 |

**ルール：**
- `plan.md` を受け取っても**すぐに実装を始めない**。必ず `spec.md` を作成してユーザーに確認を取ってから着手する
- `todo.md` は `/clear` コマンド後でも作業を再開できるように、完了・進行中・次のステップを常に最新状態に保つ
- エラー・バグ・仕様の罠を発見したら `knowledge.md` に**即追記**する

# 情報の永続化（「覚えておいて」ルール）

ユーザーが「覚えておいて」「保存して」「反映して」「記録して」等と言った場合、情報の性質とスコープに応じて最適な保存先を自動判断し、反映する。

## 判断フロー

1. **スコープの判断**: 他プロジェクトでも使える汎用知識か、今のプロジェクト固有か？
2. **種別の判断**: ルール・ガイドライン / ナレッジ・学び / 技術設定 / ワークフロー？
3. **既存ファイルとの統合**: 該当する既存のセクションやskillがあれば追記。なければ新規作成。

## 保存先マトリクス

| スコープ → | ユーザー（全プロジェクト共通） | プロジェクト（リポジトリ固有） |
|---|---|---|
| ルール・ガイドライン | `~/.claude/CLAUDE.md` | `<repo>/AGENTS.md` |
| ナレッジ・学び | `~/.claude/skills/kb-*/SKILL.md`（既存 or 新規） | `works/<work>/knowledge.md` |
| 技術設定（permissions等） | `~/.claude/settings.json` | `.claude/settings.local.json` |
| ワークフロー・手順 | `~/.claude/skills/*/SKILL.md` | `works/` 配下 or プロジェクトskill |

## 反映後の報告

保存したら「どこに・何を書いたか」を簡潔に報告する。

# 判断・評価のルール

- 「変更不要」「問題なし」と結論する前に、必ずその根拠を明示すること。ドキュメントやソースを確認せずに「正確です」と言わない
- パッケージ・ライブラリを選定するときは、選んだ理由と代替案を必ず示す
